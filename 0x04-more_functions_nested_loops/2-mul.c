/**
 * mul - multiplies two integers
 * @a: receive first data
 * @b: receive second data
 * Return: c multiplication result a and b
 */
int mul(int a, int b)
{
	int c = 0;

	c = a * b;
	return (c);
}
